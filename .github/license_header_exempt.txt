# Files exempt from the AGPL-3.0-or-later header check
# (`.github/workflows/license-header.yml`).
#
# Format: one fnmatch glob per line. Comments start with `#`. Globs
# match the file path RELATIVE TO THE REPO ROOT, as `git diff
# --name-only` reports them.
#
# Keep this list short and justified. Everything in AGPL core MUST
# carry the header; only vendored/upstream code is exempt.

# ---- vendored matbot runtime (Apache-2.0 submodule) ------------------------
# external/matbot is MatAtBread/matbot, vendored as a git submodule and
# licensed Apache-2.0 (its own LICENSE is preserved in-tree). It is not
# AGPL eidan core, so its files must NOT carry the eidan header.
external/matbot/**

# vendored third-party (ponytail "lazy senior dev" ruleset, MIT — carries its own LICENSE)
packages/sage/vendor/**
