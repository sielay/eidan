"""eidan_capture — core plugin that registers the write-side tools.

Three tools: ``remember`` (knowledge), ``note`` (working memory),
``event`` (calendar items). Each filters on the contextvar identity
the loop sets at the top of every turn.
"""

__version__ = "0.1.0"
