[project]
name = "eidan-sentry"
version = "0.1.0"
description = "Eidan core plugin — Sentry continuous-thinking loop."
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["eidan_sentry"]
