# Added by code-review-graph
.code-review-graph/
.DS_Store
# Claude Code creates worktrees here for parallel branches.
.claude/worktrees/

# Local config / secrets
.env
.env.local
~/.eidan/
.eidan/
.eidan-runtime/

# Fly.io — the operator's gitignored fly.toml lives at the repo root.
/fly.toml

# Node / pnpm
node_modules/
.next/
out/
.pnpm-store/
.turbo/
logs/

# matbot runtime (operator-private). The host config + its runtime state live
# next to it; only matbot.yaml.example is tracked.
/matbot.yaml
/.data/
/.plugins/

# local dev (not for the repo)
matbot.local.yaml
/dev-up.sh
/dev-down.sh

# vendored paid bundles (private; deploy copies them in, never committed to core)
packages/sage/
packages/pro/
packages/charles/
packages/charlotte/

# operator deploy config (per-deploy; targets + bundle paths)
eidan.deploy.json
.env.compose
