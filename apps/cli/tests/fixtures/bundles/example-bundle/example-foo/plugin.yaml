schema: 1

name: example-foo
version: 0.1.0
display_name: Example Foo
description: >
  Phase 4 plugin-install fixture. Discovered by the CLI's local install
  path; the on-disk shape is the only thing the install path cares about.

tier: core
license: AGPL

bundle:
  name: example-bundle
  kind: thematic

host:
  eidan: ">=0.1.0"
  python: ">=3.11"

backend:
  entrypoint: example_foo.plugin:Plugin
