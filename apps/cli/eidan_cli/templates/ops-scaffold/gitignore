# Vault password — never commit. Operator generates this locally with:
#   echo "<long-random-passphrase>" > .vault-pass && chmod 0600 .vault-pass
.vault-pass

# Runtime artefacts the CLI generates during deploy (rendered env files,
# ephemeral Ansible inventories, fly.toml). Recreated on every run, no
# need to track.
.eidan-runtime/

# OS / editor cruft
.DS_Store
*.swp
*~
