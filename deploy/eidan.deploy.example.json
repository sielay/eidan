{
  "//": "Copy to eidan.deploy.json (gitignored) and edit. Bundles are private sibling repos vendored into the build; targets are where you ship.",

  "bundles": [
    {
      "name": "sage",
      "path": "../eidan-sage/packages/sage",
      "kind": "code",
      "//": "or fetch from git instead of a local path:",
      "//git": "sielay/eidan-sage#matbot-port",
      "//subdir": "packages/sage"
    }
  ],

  "targets": {
    "local": {
      "type": "compose"
    },
    "fly": {
      "type": "fly",
      "app": "your-eidan",
      "config": "fly.toml"
    },
    "kesha": {
      "type": "compose-ssh",
      "host": "192.168.1.100",
      "user": "pi",
      "dir": "eidan",
      "registry": "ghcr.io/sielay",
      "platform": "linux/arm64"
    },
    "pi-node": {
      "//": "ssh-node: a host running the engine as a node process under systemd (no Docker). rsyncs the runtime + restarts the service; keeps the host's own matbot.yaml + .env.",
      "type": "ssh-node",
      "host": "192.168.1.100",
      "user": "pi",
      "dir": "eidan-mb",
      "service": "eidan-mb.service"
    }
  }
}
