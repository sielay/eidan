{
  "name": "@eidandev/frontend-telegram",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Inbound Telegram chat surface: long-polls getUpdates, maps an allowlisted Telegram sender to an eidan principal, and runs each message as a matbot turn (same run path as frontend-agui), replying to the chat. Bot token + allowlist come from the vault; unknown senders are rejected.",
  "matbotRuntime": ["node"],
  "engines": {
    "node": ">=24.0.0"
  },
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --test src/*.test.ts"
  },
  "dependencies": {
    "@matatbread/matbot-plugin-api": "link:../../external/matbot/packages/core/plugin-api"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.8.0"
  }
}
