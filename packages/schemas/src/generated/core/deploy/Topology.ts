// AUTO-GENERATED — do not edit by hand.
// Source: packages/schemas/schemas/**/*.schema.json
// Run `pnpm --filter @eidan/schemas gen:ts` to refresh.
import { z } from "zod"

export const Topology = z.object({ "schema": z.literal(1).describe("Topology schema version. Always 1 for this shape; bumped on breaking changes per `docs/004 §9`."), "defaults": z.any().describe("Repo-wide defaults applied per-node via deep-merge. Per-node values override. Lets the operator declare `plugin_source: gh:sielay` and the GitHub PAT once for every node instead of repeating them.").optional(), "nodes": z.record(z.any()).describe("Map of node-name → node config. Node names are operator-chosen and used by `eidan deploy --node <name>`, log lines, and the per-node telemetry view in the admin UI (`docs/024`). Each node reconciles independently; failure on one does not block the others. A zero-node topology is rejected at the parser level (`load_topology`) rather than in this schema — `minProperties: 1` codegens inconsistently across `datamodel-code-generator` versions, so the constraint lives in `apps/cli/eidan_cli/topology.py` for portability.") }).strict().describe("Operator-private deployment topology consumed by the `eidan deploy` CLI. One node per host the operator runs an eidan backend on (Pi, Fly machine, Docker container, …). Reconcilers walk this document and bring each node into the declared state — render env files, install bundles, restart services. Lives at `.eidan/topology.yml` by default; overridable via `--topology=<path>` so a private ops repo can hold it. Secrets carried as plain strings (post-decryption); the CLI accepts ansible-vault per-value tags transparently in a follow-up.")
export type Topology = z.infer<typeof Topology>
