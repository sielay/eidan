# generated by datamodel-codegen:
#   filename:  core/turn/TurnComplete.schema.json

from __future__ import annotations

from uuid import UUID

from pydantic import BaseModel, ConfigDict


class TurnComplete(BaseModel):
    """
    Payload of the terminal SSE `complete` event from POST /api/turn (docs/005 §5.5). Carries the persisted message ids so the UI can flip its optimistic placeholders onto the backend-assigned rows and re-anchor the per-turn cost counter.
    """

    model_config = ConfigDict(
        extra="forbid",
    )
    user_message_id: UUID
    """
    Id of the persisted user `messages` row for this turn.
    """
    assistant_message_id: UUID
    """
    Id of the persisted assistant `messages` row for this turn.
    """
