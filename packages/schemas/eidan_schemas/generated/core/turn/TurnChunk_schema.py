# generated by datamodel-codegen:
#   filename:  core/turn/TurnChunk.schema.json

from __future__ import annotations

from pydantic import BaseModel, ConfigDict


class TurnChunk(BaseModel):
    """
    Payload of an SSE `chunk` event from POST /api/turn — one slice of streamed assistant text (docs/005 §5.5). Multiple chunks compose a single assistant message; the runner concatenates them into the persisted row before emitting TurnComplete.
    """

    model_config = ConfigDict(
        extra="forbid",
    )
    text: str
    """
    The incremental assistant text delta for this frame. May be empty when the provider emits structural-only chunks.
    """
