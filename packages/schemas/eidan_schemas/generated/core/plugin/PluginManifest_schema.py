# generated by datamodel-codegen:
#   filename:  core/plugin/PluginManifest.schema.json

from __future__ import annotations

from enum import StrEnum
from typing import Literal

from pydantic import BaseModel, ConfigDict, Field, RootModel


class Tier(StrEnum):
    """
    Which bundle the plugin belongs to. Metadata only — access control is enforced at the GitHub repo level (docs/001 §1.2).
    """

    core = "core"
    pro = "pro"
    commercial = "commercial"


class Kind(StrEnum):
    """
    Whether the bundle is a thematic bundle or the universal paid baseline. The CLI auto-removes baseline bundles when no thematic bundle remains installed (docs/018 §3).
    """

    thematic = "thematic"
    baseline = "baseline"


class Bundle(BaseModel):
    """
    Bundle membership metadata. Omitted for `tier: core` plugins that ship in this repo. Set on every plugin shipped in a paid bundle so the CLI can scope `plugin remove <bundle>` and reason about baseline auto-removal (docs/018 §3).
    """

    model_config = ConfigDict(
        extra="forbid",
    )
    name: str = Field(..., max_length=64, min_length=1, pattern="^[a-z0-9][a-z0-9-]*$")
    """
    Slug of the bundle this plugin ships in. Used by `eidan admin plugin remove <bundle>` to scope a multi-plugin removal (docs/018 §3).
    """
    kind: Kind | None = "thematic"
    """
    Whether the bundle is a thematic bundle or the universal paid baseline. The CLI auto-removes baseline bundles when no thematic bundle remains installed (docs/018 §3).
    """


class Author(BaseModel):
    model_config = ConfigDict(
        extra="forbid",
    )
    name: str
    email: str | None = None


class DependsOnItem(BaseModel):
    model_config = ConfigDict(
        extra="forbid",
    )
    name: str = Field(..., pattern="^[a-z0-9][a-z0-9-]*$")
    version: str = Field(..., min_length=1)
    """
    PEP 440 / npm-style version range.
    """


class Host(BaseModel):
    """
    Required host capabilities. Version strings are PEP 440 / npm-style ranges.
    """

    model_config = ConfigDict(
        extra="forbid",
    )
    eidan: str | None = None
    python: str | None = None
    node: str | None = None


class EnvItem(BaseModel):
    model_config = ConfigDict(
        extra="forbid",
    )
    name: str = Field(..., pattern="^[A-Z][A-Z0-9_]*$")
    """
    Env var name in UPPER_SNAKE_CASE.
    """
    required: bool | None = False
    default: str | None = None
    description: str | None = None


class VaultItem(BaseModel):
    model_config = ConfigDict(
        extra="forbid",
    )
    key: str = Field(..., min_length=1)
    """
    Dotted vault key the plugin reads.
    """
    required: bool | None = False
    description: str | None = None


class Backend(BaseModel):
    """
    Backend extension point. Omitted when the plugin contributes no Python code.
    """

    model_config = ConfigDict(
        extra="forbid",
    )
    entrypoint: str = Field(
        ..., pattern="^[A-Za-z_][A-Za-z0-9_.]*:[A-Za-z_][A-Za-z0-9_]*$"
    )
    """
    Python module:ClassName tuple resolving to the Plugin subclass (docs/001 §2.2).
    """
    routes_prefix: str | None = Field(None, pattern="^/")
    """
    URL prefix the plugin's FastAPI routers mount under (docs/001 §2.2).
    """


class Route(BaseModel):
    model_config = ConfigDict(
        extra="forbid",
    )
    path: str
    component: str


class Component(BaseModel):
    model_config = ConfigDict(
        extra="forbid",
    )
    slot: str
    component: str


class Frontend(BaseModel):
    """
    Frontend extension point. Omitted when the plugin contributes no UI.
    """

    model_config = ConfigDict(
        extra="forbid",
    )
    package: str | None = None
    """
    Path to the plugin's web package, relative to the plugin root.
    """
    routes: list[Route] | None = None
    components: list[Component] | None = None
    settings_panel: str | None = None


class Driver(StrEnum):
    alembic = "alembic"
    sql = "sql"


class Migrations(BaseModel):
    """
    Migration extension point (docs/001 §4).
    """

    model_config = ConfigDict(
        extra="forbid",
    )
    dir: str
    """
    Path to the migrations directory, relative to the plugin root.
    """
    driver: Driver | None = "alembic"


class Kind1(StrEnum):
    """
    Dispatch shape (docs/026). `llm_turn` (default) — host wraps the handler in the full run_turn loop. `tool_chain` — handler is a deterministic coroutine that calls tools directly. `classifier_gate` — coroutine plus one classifier call at a branch point. `notify` — no agent work, just emit a side effect. Additive: unset means `llm_turn` and behaves as today.
    """

    llm_turn = "llm_turn"
    tool_chain = "tool_chain"
    classifier_gate = "classifier_gate"
    notify = "notify"


class Behaviour(BaseModel):
    model_config = ConfigDict(
        extra="forbid",
    )
    id: str = Field(..., min_length=1)
    """
    Globally unique, plugin-prefixed behaviour id.
    """
    trigger: str = Field(..., pattern="^(event|cron|webhook|schedule|agent):.+$")
    """
    Trigger spec — one of event:/cron:/webhook:/schedule:/agent: (docs/001 §5.1).
    """
    handler: str = Field(
        ..., pattern="^[A-Za-z_][A-Za-z0-9_.]*:[A-Za-z_][A-Za-z0-9_]*$"
    )
    """
    Python module:function reference for the handler.
    """
    kind: Kind1 | None = "llm_turn"
    """
    Dispatch shape (docs/026). `llm_turn` (default) — host wraps the handler in the full run_turn loop. `tool_chain` — handler is a deterministic coroutine that calls tools directly. `classifier_gate` — coroutine plus one classifier call at a branch point. `notify` — no agent work, just emit a side effect. Additive: unset means `llm_turn` and behaves as today.
    """


class Transport(StrEnum):
    stdio = "stdio"
    sse = "sse"


class Tool(RootModel[str]):
    root: str = Field(..., min_length=1)


class Mcp(BaseModel):
    """
    Optional MCP server stanza (docs/001 §7).
    """

    model_config = ConfigDict(
        extra="forbid",
    )
    enabled: bool
    name: str | None = Field(None, pattern="^[a-z0-9][a-z0-9-]*$")
    entrypoint: str | None = Field(
        None, pattern="^[A-Za-z_][A-Za-z0-9_.]*:[A-Za-z_][A-Za-z0-9_]*$"
    )
    transport: Transport | None = None
    tools: list[Tool] | None = None
    """
    Public allowlist of tool names externally exposed via MCP (docs/001 §7).
    """


class Adapter(BaseModel):
    model_config = ConfigDict(
        extra="forbid",
    )
    channel: str = Field(
        ..., max_length=32, min_length=1, pattern="^[a-z0-9][a-z0-9_-]*$"
    )
    """
    Channel slug consumed by ctx.notify(channel=...). MUST be unique across all loaded plugins; the host registers each channel exactly once.
    """
    factory: str = Field(
        ..., pattern="^[A-Za-z_][A-Za-z0-9_.]*:[A-Za-z_][A-Za-z0-9_]*$"
    )
    """
    Python entrypoint (module:function) of the adapter factory. The factory takes a SecretAccessor coroutine and returns a NotificationAdapter; the host calls it once at activation and registers the result on the process-local NotificationRouter.
    """


class Notifications(BaseModel):
    """
    Outbound notification adapters this plugin contributes (docs/001 §6, docs/024).
    """

    model_config = ConfigDict(
        extra="forbid",
    )
    adapters: list[Adapter] = Field(..., min_length=1)


class PluginManifest(BaseModel):
    """
    Shape of plugins/<name>/plugin.yaml — the single source of truth for a plugin's identity, dependencies, declared access, and extension points (docs/001 §1).
    """

    model_config = ConfigDict(
        extra="forbid",
    )
    schema_: Literal[1] = Field(..., alias="schema")
    """
    Manifest schema version. Always 1 for the docs/001 §1.1 shape.
    """
    name: str = Field(..., max_length=64, min_length=1, pattern="^[a-z0-9][a-z0-9-]*$")
    """
    Globally unique slug used as the plugin's identifier in logs, DB rows, and the plugin_<name_underscored> schema. The plugins/<name>/ directory MUST equal this value (docs/001 §1.2).
    """
    version: str = Field(..., pattern="^\\d+\\.\\d+\\.\\d+(?:[-+][A-Za-z0-9.-]+)?$")
    """
    Plugin version, semver 2.0.0. MUST be strictly increasing across installed releases (docs/001 §1.2).
    """
    display_name: str | None = None
    """
    Human-facing label shown in the plugin admin UI.
    """
    description: str | None = None
    """
    One-paragraph summary shown in the plugin admin UI.
    """
    tier: Tier
    """
    Which bundle the plugin belongs to. Metadata only — access control is enforced at the GitHub repo level (docs/001 §1.2).
    """
    bundle: Bundle | None = None
    """
    Bundle membership metadata. Omitted for `tier: core` plugins that ship in this repo. Set on every plugin shipped in a paid bundle so the CLI can scope `plugin remove <bundle>` and reason about baseline auto-removal (docs/018 §3).
    """
    license: str
    """
    SPDX licence identifier.
    """
    authors: list[Author] | None = None
    """
    Authors / maintainers, listed in the plugin admin UI.
    """
    depends_on: list[DependsOnItem] | None = None
    """
    Other plugins required at load time. MUST form a DAG; cycles are a fatal load error (docs/001 §1.2).
    """
    host: Host | None = None
    """
    Required host capabilities. Version strings are PEP 440 / npm-style ranges.
    """
    env: list[EnvItem] | None = None
    """
    Env vars the plugin reads. Reading an undeclared env var raises UndeclaredAccessError (docs/001 §1.2).
    """
    vault: list[VaultItem] | None = None
    """
    Vault keys the plugin reads. Reading an undeclared key raises UndeclaredAccessError (docs/001 §1.2).
    """
    backend: Backend | None = None
    """
    Backend extension point. Omitted when the plugin contributes no Python code.
    """
    frontend: Frontend | None = None
    """
    Frontend extension point. Omitted when the plugin contributes no UI.
    """
    migrations: Migrations | None = None
    """
    Migration extension point (docs/001 §4).
    """
    behaviours: list[Behaviour] | None = None
    """
    Agentic-loop behaviours the plugin registers (docs/001 §5).
    """
    mcp: Mcp | None = None
    """
    Optional MCP server stanza (docs/001 §7).
    """
    notifications: Notifications | None = None
    """
    Outbound notification adapters this plugin contributes (docs/001 §6, docs/024).
    """
