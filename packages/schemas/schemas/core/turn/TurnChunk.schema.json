{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.eidan.dev/core/turn/TurnChunk/v1.json",
  "title": "TurnChunk",
  "description": "Payload of an SSE `chunk` event from POST /api/turn — one slice of streamed assistant text (docs/005 §5.5). Multiple chunks compose a single assistant message; the runner concatenates them into the persisted row before emitting TurnComplete.",
  "type": "object",
  "additionalProperties": false,
  "required": ["text"],
  "properties": {
    "text": {
      "type": "string",
      "description": "The incremental assistant text delta for this frame. May be empty when the provider emits structural-only chunks."
    }
  }
}
