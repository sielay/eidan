{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.eidan.dev/core/admin/NodeList/v1.json",
  "title": "NodeList",
  "description": "Response shape of GET /api/admin/nodes (docs/024 §5.1). One row per registered backend process; ordered by last_seen DESC so live nodes float to the top. The UI uses seconds_since to render the heartbeat-freshness dot (≤90s green / ≤600s amber / else red per docs/024 §1.2).",
  "type": "object",
  "additionalProperties": false,
  "required": ["nodes"],
  "properties": {
    "nodes": {
      "type": "array",
      "description": "Zero or more registered nodes. Empty array is a valid response on a fresh deployment.",
      "items": { "$ref": "#/$defs/NodeInfo" }
    }
  },
  "$defs": {
    "NodeInfo": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "node_id",
        "node_type",
        "status",
        "last_seen",
        "seconds_since",
        "metadata",
        "plugins"
      ],
      "properties": {
        "node_id": {
          "type": "string",
          "minLength": 1,
          "description": "Stable per-process identifier from docs/024 §1."
        },
        "node_type": {
          "type": "string",
          "enum": ["pi", "fly", "heroku", "k8s", "local"],
          "description": "Platform classification — matches the CHECK on eidan.node_heartbeats.node_type."
        },
        "status": {
          "type": "string",
          "enum": ["online", "offline", "degraded"],
          "description": "Stored row state. UI may treat as offline when seconds_since exceeds a freshness threshold regardless of the stored value (docs/024 §3 — stop() does not mark offline)."
        },
        "last_seen": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp of the last heartbeat UPSERT."
        },
        "seconds_since": {
          "type": "integer",
          "minimum": 0,
          "description": "Server-computed (now() - last_seen) in seconds so the UI does not need to negotiate clock skew."
        },
        "metadata": {
          "type": "object",
          "description": "Platform fingerprint — always carries hostname / platform / python; per-platform keys (fly_app, fly_region, …) layer on top."
        },
        "plugins": {
          "type": "array",
          "description": "Plugins active on this node when the heartbeat was last written (issue #52). Per-process — the Pi and a Fly machine can carry different sets after a runtime install. Empty array on nodes whose process predates the schema bump.",
          "items": { "$ref": "#/$defs/NodePluginInfo" }
        }
      }
    },
    "NodePluginInfo": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "version", "tier"],
      "properties": {
        "name": {
          "type": "string",
          "minLength": 1,
          "description": "Plugin manifest name — the on-disk directory name under plugins/."
        },
        "version": {
          "type": "string",
          "minLength": 1,
          "description": "Plugin manifest version string (semver-ish; the host does not validate the shape here)."
        },
        "tier": {
          "type": "string",
          "enum": ["core", "pro", "commercial"],
          "description": "Which bundle the plugin ships in. Mirrors plugin.yaml's tier field."
        }
      }
    }
  }
}
